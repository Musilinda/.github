# Prod environment — full-size box, apex domain. Committed (no secrets here).
env               = "prod"
domain            = "musilinda.com"
bundle_id         = "large_2_0" # 8 GB (~$44/mo)
aws_region        = "us-east-1"
availability_zone = "us-east-1a"
